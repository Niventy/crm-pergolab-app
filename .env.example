# ----------------------------------------------------------------------------
# CRM Pergolab — variables d'environnement
# Copier ce fichier en `.env.local` et remplir les valeurs.
# NE JAMAIS committer `.env.local` ni de vraie clé.
# ----------------------------------------------------------------------------

# --- Supabase (client navigateur) ------------------------------------------
# Dashboard Supabase > Project Settings > Data API / API Keys
NEXT_PUBLIC_SUPABASE_URL="https://xxxxxxxxxxxxxxxx.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJhbGci...."

# --- Supabase (serveur uniquement, NE PAS exposer au client) ----------------
# Dashboard Supabase > Project Settings > API Keys > service_role
# Sert aux opérations serveur (webhook entrant) qui contournent la RLS.
SUPABASE_SERVICE_ROLE_KEY="eyJhbGci...."

# --- Base de données (Drizzle migrations / seed) ----------------------------
# Dashboard Supabase > Project Settings > Database > Connection string
# Utiliser la connexion "Session"/"Direct" (port 5432) pour les migrations.
# Le mot de passe est celui défini à la création du projet Supabase.
DATABASE_URL="postgresql://postgres:[PASSWORD]@db.xxxxxxxxxxxxxxxx.supabase.co:5432/postgres"

# --- Webhook entrant (Meta Lead Ads via Zapier / Make) ----------------------
# Secret partagé attendu dans l'en-tête: Authorization: Bearer <SECRET>
# Générer une valeur aléatoire longue, ex: openssl rand -hex 32
INBOUND_WEBHOOK_SECRET="changez-moi-par-une-valeur-aleatoire-longue"

# --- Envoi d'emails via Gmail (OAuth) ---------------------------------------
# Le CRM envoie depuis un compte d'envoi @pergolab.fr autorisé via OAuth.
# Mise en place (droits "Propriétaire de projet" suffisent) :
#   1) console.cloud.google.com > projet > activer "Gmail API".
#   2) APIs & services > Écran de consentement OAuth : User type = Interne.
#   3) Identifiants > Créer un ID client OAuth (type "Application Web"),
#      Redirect URI = https://developers.google.com/oauthplayground
#   4) developers.google.com/oauthplayground > roue crantée > "Use your own
#      OAuth credentials" (colle client id + secret) > scopes :
#        https://www.googleapis.com/auth/gmail.send
#        https://www.googleapis.com/auth/gmail.readonly   (lecture du fil)
#        https://www.googleapis.com/auth/calendar.events  (Google Agenda)
#      > Authorize (connecte-toi avec L'ADRESSE de l'expéditeur, ex. sofiane@…,
#      pas une autre — le token est lié au compte choisi) > Exchange > copie le
#      "refresh token". NB : Gmail/Agenda agissent sur la boîte du TOKEN.
GOOGLE_CLIENT_ID="xxxxx.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="GOCSPX-xxxxx"
# --- Expéditeur unique (le plus simple) ---
GOOGLE_REFRESH_TOKEN="1//xxxxx"
GOOGLE_SENDER="Pergolab <adv@pergolab.fr>"
# --- OU plusieurs expéditeurs (1 refresh token par adresse, via OAuth Playground
#     connecté avec CETTE adresse). `login` = email de connexion au CRM ; l'envoi
#     part de `from`. Si renseigné, prend le pas sur GOOGLE_SENDER/_REFRESH_TOKEN. ---
# GOOGLE_SENDERS='[{"login":"sofianekrif@pergolab.fr","from":"Sofiane Krif <sofianekrif@pergolab.fr>","refreshToken":"1//aaa"},{"login":"adv@pergolab.fr","from":"Pergolab <adv@pergolab.fr>","refreshToken":"1//bbb"}]'

# --- Pennylane (facturation) ------------------------------------------------
# Section « Devis » de la fiche : on compose les lignes dans le CRM puis on clique
# « Créer dans Pennylane » (API externe v2) → crée le client + le devis.
# Générer un "Company API Token" dans Pennylane > Paramètres > API / Intégrations
# (scopé sur TON entreprise ; via Keobiz si l'accès est géré par le cabinet).
# Absent => le bouton affiche « non configuré » (aucun appel).
PENNYLANE_API_KEY="votre-company-api-token"
# Ouverture de l'éditeur de devis dans l'app Pennylane (hand-off). L'id société
# est lu via /me, sinon fixe-le ici. L'URL de l'éditeur n'étant pas documentée,
# ajuste le pattern si besoin (jetons {company} et {quote}).
# PENNYLANE_COMPANY_ID="123456"
# PENNYLANE_QUOTE_URL="https://app.pennylane.com/companies/{company}/quotes/{quote}"
